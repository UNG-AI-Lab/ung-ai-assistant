# UNG AI Assistant — Backend

FastAPI backend for the UNG AI Academic Assistant.

## Setup

```bash
poetry install
poetry run uvicorn app.main:app --reload
```

## API Documentation

Once running, visit:
- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`
